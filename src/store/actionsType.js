/**
 * @description store의 각 action별 action type 관리
 */

const commonActionType = {
	ACTION_SAMPLE: 'ACTION_SAMPLE',
};

const modules1ActionType = {
	ACTION_SAMPLE: 'ACTION_SAMPLE',
};

export { commonActionType, modules1ActionType };
